# DESIGN.md — IEP GEN

> สัญญาด้านภาพของโปรเจกต์ ใช้ร่วมกันระหว่าง Claude Code และ Claude Design
> ขอบเขต: **ชั้น visual + responsive เท่านั้น** ไม่แตะ flow, logic, `lib/types.ts`, `app/api/**`, `prisma/**`
> ถ้าไฟล์นี้ขัดกับ `CLAUDE.md` → `CLAUDE.md` ชนะ
> ข้อมูล "ในโค้ดตอนนี้" ทั้งไฟล์ตรวจด้วย grep เมื่อ **13 ก.ย. 2569** — เลขบรรทัดเลื่อนได้ ให้ grep ข้อความซ้ำก่อนเชื่อ

---

## 1. Subject & audience

**IEP GEN คืออะไร** — เว็บแอปช่วยร่างแผนการจัดการศึกษาเฉพาะบุคคล (IEP) ของโรงเรียนไทย
กรอกข้อมูลนักเรียนครั้งเดียว → เลือกระดับความสามารถ → ระบบร่างเป้าหมายและรายการสื่อที่เบิกได้
→ ครูตรวจแก้และยืนยัน → คัดลอกไปวางในระบบ SET / ระบบคูปอง · export เป็น .docx

**ผู้ใช้ 2 กลุ่ม — ออกแบบให้กลุ่มที่ 2 ใช้ได้เป็นหลัก**

| | ครูการศึกษาพิเศษ | พี่เลี้ยงเด็กพิการ (ผู้ใช้หลักของการออกแบบ) |
| --- | --- | --- |
| วุฒิ | ป.โท | ม.6 ไม่มีพื้นฐานการสอน |
| ความรู้ศัพท์ | รู้ศัพท์เทคนิค (IEP, ตัวชี้วัด, บัญชี ก/ข) | ไม่คุ้นศัพท์เทคนิค |
| สิ่งที่ต้องการ | ทำเสร็จเร็ว แก้ได้ทุกจุด | รู้ว่าแต่ละช่องต้องกรอกอะไร ไม่กลัวกดผิด |

ผลต่อการออกแบบ:

- **ทุกช่องกรอกมี label + คำอธิบายสั้นใต้ label** (hint) เป็นภาษาพูดธรรมดา
  ศัพท์เทคนิคใช้ได้ แต่ต้องมี hint อธิบายกำกับ
- ปุ่มบอกผลลัพธ์เป็นคำกริยาชัดเจน ("สร้างแผน", "ยืนยันแผน", "คัดลอกไปวางในระบบ SET") ไม่ใช่ "ตกลง" / "ส่ง"
- สถานะต้องอ่านออกจากคำ ไม่ใช่จากสีอย่างเดียว (สี + ข้อความ/ไอคอน เสมอ)
- ครูตัดสินทุกจุด — UI ต้องทำให้เห็นชัดว่า "นี่คือสิ่งที่ AI เสนอ" กับ "นี่คือสิ่งที่ครูยืนยันแล้ว"

**บริบทการใช้งาน**

- **Desktop ที่โรงเรียน** (อ้างอิง 1280px) — จอไม่ใหญ่ เครื่องไม่แรง เน็ตโรงเรียนช้าได้
- **มือถือ** (อ้างอิง 390px) — กรอกระหว่างวัน นิ้วโป้งกดเป็นหลัก

**ทิศทางภาพ** — ภาษาภาพจาก **เอกสารราชการไทย**: กระดาษขาว หมึกดำ เส้นบรรทัด กรอบตาราง
หมึกน้ำเงินตรงจุดที่ต้องลงนาม/ตัดสินใจ ความน่าเชื่อถือมาจากความเป็นระเบียบ ไม่ใช่จากเงาหรือ gradient

---

## 2. Color tokens

7 ค่าเท่านั้น **ห้ามใช้ palette default ของ Tailwind** (`slate-*`, `gray-*`, `teal-*`, `blue-600`, `amber-*`, `red-*` ฯลฯ) ในงานใหม่

| Token | Hex | หน้าที่ |
| --- | --- | --- |
| `paper` | `#FDFCF9` | พื้นหลังหน้า, พื้นการ์ด, พื้นช่องกรอก, หน้าเอกสาร — กระดาษแผ่นเดียวทั้งแอป |
| `ink` | `#1C2233` | ข้อความหลัก, heading, ค่าที่กรอก |
| `muted` | `#5A6072` | label รอง, hint, placeholder, **เส้นขอบ control ทุกชนิด** (ใช้ผ่านชื่อ `border-control`) |
| `rule` | `#D6D1C4` | เส้นบรรทัด, เส้นขอบการ์ด, เส้นคั่น section — เส้นตกแต่ง/จัดกลุ่มเท่านั้น |
| `accent` | `#1F4E9E` | "หมึกน้ำเงินลงนาม" — ปุ่มหลัก, ลิงก์, focus ring, สถานะที่ **ครูแตะ/เลือก/ยืนยันเองแล้ว** |
| `attention` | `#8A5300` | **warning** — ต้องให้ครูดูและตัดสินเอง: ความไม่สอดคล้อง, "แก้ไขจากที่ AI ร่าง", คำเตือน PII |
| `danger` | `#9E2B25` | **error เท่านั้น** (แดงตราครั่ง) — ระบบทำงานไม่สำเร็จ: classifier พัง, DB ต่อไม่ติด, บันทึกไม่สำเร็จ |

**เส้นขอบ 2 ตัวหน้าตาใกล้กัน — ห้ามหยิบผิด**

- `border-rule` = ขอบการ์ดและเส้นคั่นเท่านั้น
- `border-control` = ขอบ input ทุกชนิด (`input`, `textarea`, `select`) — WCAG 1.4.11 ต้องการ 3:1 สำหรับขอบ control และ `rule` อยู่ที่ประมาณ 1.5:1 ซึ่งไม่ผ่าน
  - `border-control` กับ `border-muted` ได้สีเดียวกัน (= `muted`) แต่ **ให้ใช้ `border-control`** เพราะชื่อบอกหน้าที่ ไม่ใช่บอกสี
    ถ้าวันหนึ่งสีขอบ control ต้องต่างจากสีข้อความรอง แก้ที่ `tailwind.config.ts` (`borderColor.control`) จุดเดียว

**Contrast บน `paper` #FDFCF9** (AA ข้อความปกติ ≥ 4.5:1 · AAA ≥ 7:1)

| Token | Contrast | ระดับ |
| --- | --- | --- |
| `ink` #1C2233 | ≈ 15.4:1 | AA + AAA |
| `accent` #1F4E9E | 7.75:1 | AA + AAA |
| `danger` #9E2B25 | 7.24:1 | AA + AAA (ข้อความ `paper` บนพื้น `danger` ค่าเท่ากัน) |
| `attention` #8A5300 | 6.17:1 | **AA เท่านั้น ไม่ถึง AAA** |
| `muted` #5A6072 | ≈ 6.1:1 | AA เท่านั้น |
| `rule` #D6D1C4 | ≈ 1.5:1 | ไม่ผ่าน — เส้นตกแต่งเท่านั้น |

### Warning กับ error ต้องแยกด้วย treatment ไม่ใช่แค่สี

relative luminance ของ `danger` = **0.0913** กับ `attention` = **0.1159** ใกล้กันมาก
คนตาบอดสีแยกด้วย hue อย่างเดียวไม่ได้ → **ห้ามพึ่งสีอย่างเดียวในการแยกสองสถานะนี้**
ครูที่พิมพ์บรรยายเด็กมา 30 นาทีต้องแยกออกทันทีว่า "ระบบชวนให้ตรวจ" กับ "งานคุณยังไม่ถูกเซฟ"

| | warning (`attention`) | error (`danger`) |
| --- | --- | --- |
| ความหมาย | ความไม่สอดคล้องที่ครูต้องตัดสินเอง — ห้าม auto-fix | ระบบทำงานไม่สำเร็จ งานอาจยังไม่ถูกบันทึก |
| รูปแบบ | พื้นอ่อน (`attention` 10%) + เส้นขอบ 1px `attention` + ⚠ | **แถบทึบ `danger` เต็มความกว้าง** ข้อความสี `paper` |
| ตำแหน่ง | inline ในตำแหน่งที่เกี่ยวข้อง (ติดช่อง/รายการที่มีปัญหา — ดู B5) | เต็มความกว้างของพื้นที่ที่ล้มเหลว |
| เนื้อหา | บอกว่าควรตรวจอะไร | **บอกว่าต้องทำอะไรต่อ** ("กดบันทึกร่างอีกครั้ง", "เลือกระดับเอง") |
| การหายไป | **อยู่ตลอด ไม่บล็อกการทำงานต่อ** | **ไม่หายเอง** — หายเมื่อทำสำเร็จจริงเท่านั้น |

**ค่าที่อนุญาตให้ derive (ไม่นับเป็น token ใหม่)** — ใช้ opacity ของ token เดิมเท่านั้น

| ใช้ทำอะไร | ค่า |
| --- | --- |
| พื้นรายการที่ครูเลือกเอง (goal/media ที่ติ๊กแล้ว, นักเรียนที่เลือก, chip ระดับที่ครูเลือกเอง) | `accent` 8% บน `paper` + border `accent` |
| พื้นกล่อง warning | `attention` 10% บน `paper` + border `attention` |
| hover ของปุ่มรอง/แถวรายการ, พื้น loading, พื้น chip tone กลาง | `ink` 4% |
| hover ของปุ่มหลัก | `accent` 90% |
| ปุ่ม disabled | พื้น `rule` + ข้อความ `muted` |

**Chip tones** (สำหรับ `components/ui/Chip.tsx`): `verified` → `accent` · `pending` (tone กลาง) → `muted` · `warning` → `attention`

- **`verified` สงวนไว้ให้ค่าที่ครูแตะจริงเท่านั้น** — ห้ามใช้กับค่าที่ AI กรอกให้แล้วครูยังไม่ได้แตะ (ดู §4.8)
- error ไม่ใช่ chip — ใช้แถบ `danger` ตามตารางด้านบน

**ตารางแทนที่ class เดิม** (ใช้ตอน Phase 5 ไล่แก้ component — ครอบทุก class ที่ grep เจอ ณ 13 ก.ย. 2569)

| class เดิม | แทนด้วย |
| --- | --- |
| `bg-white`, `bg-slate-50` (body/พื้นการ์ด/พื้นกล่องย่อย) | `bg-paper` (ถ้าต้องจัดกลุ่ม ใช้กรอบ `rule` แทนพื้นสี) |
| `hover:bg-slate-50`, `bg-slate-100` | `ink` 4% |
| `text-slate-900` / `-800` / `-700`, `hover:text-slate-700` | `text-ink` |
| `text-slate-600` / `-500` / `-400`, `text-gray-500` / `-400`, `hover:text-slate-600`, `disabled:text-slate-400` | `text-muted` |
| `border-slate-200` (การ์ด, เส้นคั่น), `border-dashed border-slate-300` (กล่อง empty state) | `border-rule` |
| `border-slate-300`, `hover:border-slate-300` บน **control** (input/textarea/select/ปุ่มรอง) | `border-control` (สีเดียวกับ `border-muted` แต่ใช้ชื่อนี้ — ดู §2 "เส้นขอบ 2 ตัว") |
| `bg-teal-600`, `hover:bg-teal-700` | `bg-accent` / `accent` 90% |
| `text-teal-600` / `-700` / `-800` / `-900`, `hover:text-teal-700` / `-800`, `border-teal-500` / `-300` / `-200`, `focus:ring-teal-500`, `border-t-teal-500` (spinner) | `accent` — **ยกเว้น chip AI กรอกให้ใน `AssessmentForm.tsx` ~279–301 ดู §4.8** |
| `bg-teal-50`, `bg-teal-50/40`, `bg-teal-100` | `accent` 8% — ยกเว้นเดียวกัน |
| `text-amber-600` … `-950`, `bg-amber-50`, `border-amber-200` / `-300` | `attention` / `attention` 10% |
| `text-red-600` / `-800`, `bg-red-50`, `border-red-200` | แถบ `bg-danger` + `text-paper` |
| `bg-slate-800` (ปุ่มบันทึกนักเรียน) | `bg-accent` |
| `disabled:bg-slate-300` | พื้น `rule` + ข้อความ `muted` |
| `text-white` (บนปุ่มหลัก/แถบ error) | `text-paper` |

---

## 3. Typography

### ฟอนต์ — ตัวเดียว

| บทบาท | ฟอนต์ | ใช้ที่ไหน |
| --- | --- | --- |
| **UI** | **IBM Plex Sans Thai Looped** (400 / 500 / 600 / 700) | ทุกอย่างในแอป: label, ปุ่ม, ช่องกรอก, heading, ข้อความ |

- **700 เป็นค่าถาวร** — `font-bold` ถูกใช้จริงหลายจุด ถ้าไม่โหลด เบราว์เซอร์จะทำ faux bold ซึ่งกับฟอนต์ไทยเส้นจะบวมและหัวตัวอักษรเสียรูป

- ใช้แบบ **มีหัว (Looped)** เพราะอ่านง่ายกว่าสำหรับผู้อ่านทั่วไปและตัวเล็กบนมือถือ
- **Phase 2.1 โหลดฟอนต์ตัวเดียว** ด้วย `next/font/google` subsets `['thai','latin']`, `display: 'swap'` ผูกเป็น CSS variable
  — ฟอนต์ไทยมี subset หนัก ไม่จ่ายค่าโหลดให้พื้นที่ที่ยังไม่มี
- fallback: `"IBM Plex Sans Thai Looped", "Noto Sans Thai Looped", system-ui, sans-serif`
- **สถานะปัจจุบัน:** `app/globals.css:6` ยังตั้ง `body { font-family: "Sarabun", "Noto Sans Thai", system-ui, sans-serif; }`
  และ `app/layout.tsx` ยังไม่โหลดฟอนต์ใดๆ → **Phase 2.1 ต้องลบบรรทัด font-family Sarabun นี้ออก** แล้วชี้ไปที่ตัวแปรของ Plex

> **หมายเหตุ Sarabun** — จะเพิ่มตอนสร้างพื้นที่พรีวิวเอกสาร (กอง B ใน §6) เท่านั้น ห้ามโหลดล่วงหน้า
> ไฟล์ .docx ใช้ `"TH Sarabun New"` ของเครื่องผู้เปิดอยู่แล้ว (`app/api/plans/[id]/export/route.ts:57`) ไม่เกี่ยวกับฟอนต์เว็บ

### Line-height ภาษาไทย (ห้ามต่อรอง)

- **body / ข้อความทุกชนิด / label / hint / ช่องกรอก: `1.75`**
- **heading: `1.4`**
- **ห้ามใช้ `leading-tight`, `leading-none`, `leading-snug` กับข้อความไทย** — สระบน/วรรณยุกต์/สระล่างจะชนบรรทัดข้างเคียง
- ตั้ง `line-height: 1.75` ที่ `body` เป็นค่าฐาน แล้วให้ heading override เป็น 1.4

### Type scale

ขนาดต่ำสุดของข้อความไทยคือ **14px** — ห้ามใช้ `text-xs` (12px) กับข้อความไทย
(ณ 13 ก.ย. 2569 โค้ดใช้ `text-xs` **50 จุด** ส่วนใหญ่เป็น label/hint → ย้ายไป `label` / `hint` · ผลกระทบดู §7)

| Token | ขนาด | line-height | น้ำหนัก | สี | ใช้กับ |
| --- | --- | --- | --- | --- | --- |
| `display` | 28px (มือถือ 24px) | 1.4 | 600 | `ink` | ชื่อหน้า (h1) — 1 ครั้งต่อหน้า |
| `title` | 20px | 1.4 | 600 | `ink` | หัวการ์ด/หัว section (h2) เช่น "นักเรียน", "6. สิ่งอำนวยความสะดวก สื่อ บริการ" |
| `subtitle` | 17px | 1.4 | 600 | `ink` | หัวข้อย่อย (h3) เช่น "ข้อ 5.1 · ด้านการสื่อสาร" |
| `body` | 16px | 1.75 | 400 | `ink` | ข้อความทั่วไป **และค่าในช่องกรอกทุกชนิด** (16px กัน iOS zoom ตอนแตะช่องกรอก) |
| `label` | 15px | 1.75 | 500 | `ink` | label ของช่องกรอก, ข้อความปุ่ม |
| `hint` | 14px | 1.75 | 400 | `muted` | คำอธิบายใต้ label, ข้อความสถานะ, หมายเหตุท้ายหน้า |
| `stat` | 28px | 1.4 | 600 | `ink` | ตัวเลขในหน้า `/stats` |

---

## 4. Layout & principles

### 4.1 เส้นบรรทัดแทนเงา

- **การ์ด = กรอบเส้น 1px `rule` บนกระดาษ `paper`** ไม่มี shadow ไม่มีพื้นสีต่างจากหน้า
- แบ่ง section ด้วยเส้นคั่น 1px `rule` (แบบบรรทัดในแบบฟอร์มราชการ) ไม่ใช่ด้วยการเว้นกล่องสีเทา
- หัว section มีเส้น `rule` ใต้หัวข้อ
- มุมโค้งเล็ก: การ์ด/ช่องกรอก/ปุ่ม `4px` · chip `9999px` — เอกสารราชการเป็นเหลี่ยม ไม่ใช้ `rounded-xl`
- **shadow ใช้ได้เฉพาะ element ที่ลอยอยู่เหนือหน้าจริง:** dropdown/popover, toast "✓ คัดลอกแล้ว",
  แถบปุ่มที่ sticky ติดขอบล่างบนมือถือ, modal — ไม่ใช้กับการ์ด ปุ่ม หรือช่องกรอก

**shadow ในโค้ดตอนนี้ (4 จุด)**

| ไฟล์:บรรทัด | class | element คืออะไร | ผล |
| --- | --- | --- | --- |
| `components/AuthMenu.tsx:97` | `shadow-lg` | แผงเมนูบัญชี `#auth-menu-panel` — `absolute right-0 top-full z-50` โผล่เมื่อ `open` (dropdown) | **อนุญาต** — ลอยจริง |
| `app/auth/signin/page.tsx:36` | `shadow-sm` | การ์ดล็อกอินกลางหน้า (`<div>` ใน `<main>` flex center) | **ไม่อนุญาต** — การ์ดไม่ได้ลอย → ต้องเป็นศูนย์ (§7) |
| `app/auth/denied/page.tsx:35` | `shadow-sm` | การ์ด "บัญชีนี้ยังไม่ได้รับสิทธิ์เข้าใช้งาน" กลางหน้า | **ไม่อนุญาต** → ต้องเป็นศูนย์ (§7) |
| `app/privacy/page.tsx:45` | `shadow-sm` | กล่องเนื้อหา `<article>` นโยบายความเป็นส่วนตัว | **ไม่อนุญาต** → ต้องเป็นศูนย์ (§7) |

### 4.2 ความกล้าใช้ที่เดียว

- **1 หน้าจอ = ปุ่มพื้นทึบ `accent` ได้ 1 ปุ่ม** คือ action หลักของขั้นนั้น
  (หน้ากรอก: "สร้างแผน" · หน้าทบทวน: "ยืนยันแผน") ปุ่มอื่นเป็นแบบกรอบเส้นหรือ ghost
- `attention` ใช้เฉพาะสิ่งที่ครูต้องตัดสิน · `danger` ใช้เฉพาะ error — ห้ามใช้ตกแต่งทั้งคู่
- ไม่มี gradient, ไม่มีภาพประกอบตกแต่ง, emoji ใช้เฉพาะที่เป็นสัญลักษณ์สถานะ (🔒 ⚠️ ✏️ ✓)

### 4.3 ช่องกรอก (Field pattern)

ลำดับจากบนลงล่างเสมอ: **label (`label`) → hint (`hint`) → ช่องกรอก → ข้อความสถานะ/ข้อผิดพลาด**

- ช่องกรอก: พื้น `paper`, สูงอย่างน้อย 44px, ข้อความ 16px
- **ขอบช่องกรอก = `border border-control` (1px สี `muted`) เสมอ ห้ามใช้ `border-rule`** (เหตุผลดู §2 "เส้นขอบ 2 ตัว")
- focus: ring 2px `accent` + offset 2px — ต้องเห็นชัดทุก interactive element (Tab ไล่ได้ทั้งหน้า)
- ช่องที่ไม่บังคับเขียน "(ไม่บังคับ)" ต่อท้าย label — ไม่ใช้ `*` อย่างเดียว
- ระยะห่างระหว่าง field `16px`

### 4.4 Spacing & sizing

- ฐาน 4px · ใน field 4–8px · ระหว่าง field 16px · ระหว่างการ์ด 24px · ระหว่าง section ใหญ่ 32px
- padding การ์ด: 16px มือถือ / 20px desktop
- **พื้นที่กดขั้นต่ำ 44×44px** ทุกปุ่ม ลิงก์ checkbox (รวม label ที่กดได้)
- **รวมถึง chip/ปุ่ม "ยืนยัน" และ "แก้" ในแถบที่ AI เสนอค่าความสามารถ** — นี่คือการกดที่สำคัญที่สุดในทั้ง flow
  และเป็นจุดที่ chip มักถูกทำเล็ก ห้ามทำเป็นลิงก์ตัวเล็ก
  ในโค้ดตอนนี้คือ (`components/AssessmentForm.tsx`) — ทุกตัวยังเป็นลิงก์ `text-xs` ต่ำกว่า 44px:
  - "ไม่ใช่? แก้ระดับ" (~บรรทัด 291–299)
  - "เลือกระดับเอง" (~บรรทัด 334–342)
  - "ให้ AI ช่วยจัดระดับ" / "ให้ AI จัดระดับใหม่อีกครั้ง" (~บรรทัด 259–268)
  - chip "ยืนยัน" — **ยังไม่มีในโค้ด** ดูกอง B6 ใน §6

### 4.5 Breakpoints (mobile-first)

| ช่วง | ความกว้าง | layout |
| --- | --- | --- |
| base | < 768px (อ้างอิง 390px) | คอลัมน์เดียว · gutter ข้าง 16px |
| `md` | ≥ 768px | คอลัมน์เดียว กว้างขึ้น · grid 2 ช่องใน field คู่ (ปีการศึกษา/ภาคเรียน) |
| `lg` | ≥ 1024px (อ้างอิง 1280px) | 2 คอลัมน์ `380px | 1fr` (ซ้าย: นักเรียน+ฟอร์ม · ขวา: ผลลัพธ์) — ตามโครงเดิมใน `app/HomeClient.tsx:131` |

- ความกว้างสูงสุดของเนื้อหา `1152px` (`max-w-6xl` เดิม) · หน้าอ่านอย่างเดียว (`/stats`, ประวัติ) `768px`
- field คู่ (`grid-cols-2`) ที่เป็นข้อความยาว เช่น ชื่อบิดา/มารดา → **คอลัมน์เดียวบนมือถือ**

### 4.6 มือถือ — 4 จุดที่ต้องออกแบบให้ถูก

1. **ตาราง/รายการสื่อหลายคอลัมน์** → กลายเป็น list การ์ด `label: value` ห้ามใช้ horizontal scroll
2. **เป้าหมาย 2–3 ตัวเลือก** → stack แนวตั้ง แต่ยังเห็นพร้อมกันว่ามีกี่ตัวเลือก (เช่น "ตัวเลือก 1/3")
3. **กรอกทีละด้าน scroll ยาว** → บอกความคืบหน้าเสมอ ("ด้าน 2/5") ให้รู้ว่ากรอกไปกี่ด้านแล้ว
4. **ปุ่ม action หลัก/ปุ่ม copy** → กดถึงได้โดยไม่ต้อง scroll กลับขึ้นไป (sticky bar ด้านล่าง — ที่เดียวที่ใช้ shadow ได้)

### 4.7 พื้นที่พรีวิวเอกสาร — ยังไม่มี (กอง B)

- เมื่อสร้าง: ฟอนต์ Sarabun (โหลดเพิ่มตอนนั้น), พื้น `paper`, กรอบ 1px `rule`, กว้างสูงสุด ~720px (สัดส่วน A4)
- เส้นบรรทัด/ตารางแบบฟอร์มจริงใช้ `rule`
- สเกลที่จะเพิ่มพร้อมกัน: `doc-body` Sarabun 16px / 1.75 / 400 · `doc-heading` Sarabun 18px / 1.4 / 700
- ห้าม Sarabun ปนใน UI และห้าม Plex ปนในพื้นที่พรีวิว
- ต้องผ่าน decision rule ใน `CLAUDE.md` ก่อนสร้าง (ช่วยครูทำ IEP เสร็จเร็วขึ้นไหม)

### 4.8 chip ระดับความสามารถ — แยก "AI จัดให้" ออกจาก "ครูเลือกเอง"

ในโค้ดตอนนี้ (`components/AssessmentForm.tsx` ~279–301) chip ใช้พื้น/ขอบ teal เดียวกันทั้งสองกรณี
ต่างกันแค่คำนำ "ระบบเข้าใจว่า:" กับ "ครูเลือกเอง:" (~282) — กรณี `confidence === "high"` (~129–135)
ค่าจาก AI ถูกกรอกเป็นค่าที่ยืนยันแล้วทันทีโดยครูยังไม่ได้แตะ

| กรณี | เงื่อนไขในโค้ด | tone | ป้ายต้องสื่อว่า |
| --- | --- | --- | --- |
| AI มั่นใจ กรอกให้ ครูยังไม่แตะ | `confirmed && !isManual` | **tone กลาง** (`pending` → `muted`, พื้น `ink` 4%, ขอบ `muted`) | **"AI จัดให้ · แตะเพื่อเปลี่ยน"** — ห้ามสื่อว่าเสร็จแล้ว |
| ครูเลือก/แก้เอง | `confirmed && isManual` | `verified` (`accent`) | "ครูเลือกเอง: …" |
| AI ไม่มั่นใจ ต้องให้ครูเลือก | `needsTeacher && !err` | `warning` (`attention`) | "⚠️ ช่วยเลือกระดับให้หน่อย" |
| classifier ล้มเหลว | `err` | แถบ `danger` (ดู B3) | บอกให้ "เลือกระดับเอง" |

- **ห้ามใช้ `verified` กับกรณีแรก** — *เหตุผล:* สีของ `verified` อ้างสถานะ "ครูตรวจแล้ว" ที่ไม่เคยเกิดขึ้น
  และพี่เลี้ยงวุฒิ ม.6 จะอ่านว่าด้านนี้เสร็จแล้ว ข้ามได้
- ทำได้ในขอบเขต visual: `isManual` มีอยู่แล้ว (~231) แค่สลับ className และข้อความป้าย
  การ "แตะเพื่อเปลี่ยน" ใช้ handler เดิมของปุ่ม "ไม่ใช่? แก้ระดับ" (~291–299) — ห้ามเพิ่ม handler หรือ state ใหม่
- ปุ่มยืนยันจริงยังเป็นเรื่องของ B6 — ข้อนี้ไม่เปลี่ยน flow

---

## 5. Do not change

สิ่งเหล่านี้คือ **หลักการของระบบ ไม่ใช่ความไม่สวยของ UI** เครื่องมือออกแบบมักเสนอให้ "ปรับให้ดีขึ้น" — ห้ามทำ
สถานะว่าแต่ละข้อ "มีในโค้ดแล้ว" หรือ "ยังต้องสร้าง" ดู §6

1. **ห้ามยุบแถบ "AI เสนอ → ครูยืนยัน" เป็น auto-accept**
   ในโค้ดตอนนี้คือ checkbox `isSelected` (เป้าหมาย, `PlanReview.tsx` GoalRow), `isApproved` (สื่อ, MediaRow),
   ปุ่ม "ยืนยันแผน" (`PlanReview.tsx` ~บรรทัด 607–616) และ chip ระดับความสามารถใน `AssessmentForm.tsx` ~บรรทัด 279–315
   **chip ของค่าที่ AI กรอกให้ (`confidence === "high"`) ห้ามใช้ tone `verified`** — ใช้ tone กลางและป้าย "AI จัดให้ · แตะเพื่อเปลี่ยน" (§4.8)
   `verified` สงวนไว้ให้ค่าที่ครูแตะจริงเท่านั้น เพราะสีนั้นอ้างสถานะที่ไม่เคยเกิด และพี่เลี้ยงวุฒิ ม.6 จะอ่านว่าข้ามได้
   *เหตุผล:* ถ้า free text ขับ retrieval ตรงๆ hallucination บนงบเบิกกลับมาเป็นไปได้ทันที
2. **ห้ามลดตัวเลือกเป้าหมายจาก 2–3 เหลือ 1** และห้ามยุบเป็น accordion/carousel ที่เห็นทีละอัน
   *เหตุผล:* ตัวเลือกคือที่ที่ครูใช้วิจารณญาณ ไม่ใช่ความซ้ำซ้อนของ UI
3. **ห้ามเอาเหตุผลกำกับคำแนะนำออก** — เหตุผลของสื่อทุกรายการ (`finalReason`) และกล่อง "ดูข้อความต้นฉบับที่ AI ร่าง" ต้องแสดงอยู่เสมอ
   *เหตุผล:* ระบบต้องไม่เป็น black box — ครูต้องเรียนรู้หลักการจากการใช้
4. **ห้ามรวมปุ่ม copy ที่ไประบบ SET กับปุ่มที่ไประบบคูปองเป็นปุ่มเดียว** — ป้ายกำกับต้องบอกชัดว่าอันไหนไประบบไหน
   *เหตุผล:* ปลายทางคนละระบบ คนละรูปแบบข้อความ
5. **ห้าม auto-fix warning ความไม่สอดคล้อง** (`consistencyWarnings`) — แสดงเป็นการแจ้งให้ครูตัดสินเท่านั้น ไม่มีปุ่ม "แก้ให้อัตโนมัติ"
   *เหตุผล:* flag ให้ครูตัดสินเท่านั้น

ข้อที่ต้องรักษาเพิ่มเติม (มาจาก `CLAUDE.md` และขอบเขตงาน visual):

6. **ห้ามเอาเครื่องหมาย "✏️ แก้ไขจากที่ AI ร่างแล้ว" / "✏️ มีการแก้ไข" ออก** — เป็นหลักฐานคู่ `aiXxx` / `finalXxx` ว่าครูแก้อะไร
7. **ห้ามเอาข้อความเตือน PII ออก ย้ายออกจากตำแหน่ง หรือทำให้เด่นน้อยลง** — มี 4 จุด:

   | ไฟล์ · ตำแหน่ง (~บรรทัด) | ข้อความ | อยู่ตรงไหนบนหน้าจอ |
   | --- | --- | --- |
   | `components/StudentPicker.tsx:134` | "🔒 ข้อมูลส่วนตัวที่กรอกจะใช้เฉพาะตอนสร้างเอกสาร ไม่ถูกส่งไปยัง AI…" | หัวฟอร์ม "+ เพิ่ม" นักเรียน (โผล่เมื่อกดเพิ่ม) |
   | `components/AssessmentForm.tsx:350` | "⚠️ ห้ามพิมพ์ชื่อจริงหรือข้อมูลระบุตัวตนของเด็กในช่องบรรยาย" | ใต้ช่องบรรยายความสามารถด้านสุดท้าย |
   | `components/AssessmentForm.tsx:367` | "⚠️ ห้ามกรอกชื่อจริงหรือข้อมูลระบุตัวตน" | ใต้ช่อง "จุดเด่น / สิ่งที่ทำได้" |
   | `app/HomeClient.tsx:173` | "⚠️ ระบบนี้ช่วยร่างเอกสารเท่านั้น ครูต้องตรวจสอบและยืนยัน…" | footer หน้าหลัก |

   ตรวจซ้ำ: `grep -rn "ไม่ถูกส่งไปยัง AI\|ห้ามพิมพ์ชื่อจริง\|ห้ามกรอกชื่อจริง\|ช่วยร่างเอกสารเท่านั้น" app components` ต้องได้ 4 บรรทัด
8. **ห้ามเปลี่ยนลำดับขั้นตอน จำนวน element และชนิดของ input**
9. **ห้ามแตะ** `lib/types.ts`, event handler / `useState` / `useEffect` / `fetch`, และไฟล์ใน `app/api/**`, `lib/**`, `prisma/**`
10. **ห้ามใส่ข้อมูลนักเรียนจริงในสกรีนช็อต mockup หรือ fixture ใดๆ** — ใช้ "นักเรียนทดสอบ ก" เท่านั้น

---

## 6. สิ่งที่ยังไม่มีในโค้ด

กฎใน §5 ปกป้องทั้งของที่มีอยู่และของที่ต้องสร้าง — agent ที่อ่านไฟล์นี้ต้องแยกให้ออก
**อย่าสรุปว่า "กฎนี้ไม่มีอะไรต้องทำ" เพราะหาของในโค้ดไม่เจอ** ให้ดูกอง B

### ผล grep ณ 13 ก.ย. 2569

| คำสั่ง | ผล |
| --- | --- |
| `grep -rn "CopyButton" app components` | **มี** — ถูกเรียกใช้ 3 จุดใน `components/PlanReview.tsx`: บรรทัด 304 (ต่อรายการสื่อ), 483 "คัดลอกเป้าหมายระยะสั้น", 518 "คัดลอกทั้งหมด" (สื่อที่อนุมัติ) |
| `grep -rn "✏️" app components` | **มี** ใน UI — `PlanReview.tsx:143` และ `:309` "✏️ แก้ไขจากที่ AI ร่างแล้ว", `:205` "✏️ มีการแก้ไข" (ที่เหลือเป็น comment ในโค้ด) |
| `grep -rn "aiOriginal\|finalText" app components` | **มี** — `PlanReview.tsx:106` แก้ `finalText`, `:151` แสดง `aiOriginal` ในกล่อง "ดูข้อความต้นฉบับที่ AI ร่าง" |
| ข้อความเตือน PII (คำสั่งใน §5 ข้อ 7) | **มี** ครบ 4 จุด |
| `grep -rn "preview\|พรีวิว" app components` | **ไม่มี** พื้นที่พรีวิวเอกสาร |

### ปุ่ม copy 3 ปุ่ม — แต่ละปุ่มส่งข้อความอะไร

| ปุ่ม | ไฟล์:บรรทัด | ข้อความที่คัดลอก | ปลายทาง |
| --- | --- | --- | --- |
| "คัดลอกเป้าหมายระยะสั้น" | `PlanReview.tsx:483–486` | `selectedGoalsText` (`:431–436`) — `finalText` ของทุกเป้าหมายที่ติ๊ก `isSelected` ข้ามทุก section คั่นด้วยบรรทัดว่าง | ระบบ SET |
| ปุ่ม "คัดลอก" ต่อรายการสื่อ | `PlanReview.tsx:304` | `mediaCopyText(media)` (`:263–266`) ของ **รายการนั้นรายการเดียว** 1 บรรทัด: `[รหัส] ชื่อสื่อ (บัญชี X, ราคา) — เหตุผล: …` (ไม่ได้เช็ค `isApproved` — ดู B8) | ระบบคูปอง |
| "คัดลอกทั้งหมด" (ส่วนที่ 6) | `PlanReview.tsx:518–521` | `approvedMediaText` (`:439–442`) — `mediaCopyText` ของ **ทุกรายการที่ติ๊ก `isApproved`** คั่นด้วยบรรทัดว่าง | ระบบคูปอง |

**ยืนยันแล้ว: ข้อความชุดเดียวของระบบคูปองไม่ได้ถูกหั่นเป็นสองปุ่ม** — "คัดลอกทั้งหมด" ให้ชุดเต็มในปุ่มเดียว
ส่วนปุ่มต่อรายการเป็นทางลัดคัดลอกทีละบรรทัด ใช้ฟังก์ชัน `mediaCopyText` เดียวกัน รูปแบบตรงกันทุกตัวอักษร จึงไม่ต้องรวมกลับ

### กอง A — ห้ามลบของที่มีอยู่

| ของ | ตำแหน่ง | กฎ §5 |
| --- | --- | --- |
| ปุ่ม copy ปลายทาง SET: "คัดลอกเป้าหมายระยะสั้น" | `PlanReview.tsx:483` | ข้อ 4 |
| ปุ่ม copy ปลายทางคูปอง: "คัดลอกทั้งหมด" + ปุ่มคัดลอกต่อรายการสื่อ | `PlanReview.tsx:518`, `:304` | ข้อ 4 |
| ✏️ "แก้ไขจากที่ AI ร่างแล้ว" / "มีการแก้ไข" | `PlanReview.tsx:143`, `:205`, `:309` | ข้อ 6 |
| กล่อง "ดูข้อความต้นฉบับที่ AI ร่าง" | `PlanReview.tsx:147–152`, `:311–321` | ข้อ 3 |
| เหตุผลสื่อ `finalReason` ทุกรายการ | `PlanReview.tsx:293–301` | ข้อ 3 |
| checkbox `isSelected` / `isApproved`, ปุ่ม "ยืนยันแผน" | `PlanReview.tsx` GoalRow, MediaRow, `:607–616` | ข้อ 1 |
| กล่อง `consistencyWarnings` (ไม่มีปุ่มแก้อัตโนมัติ) | `PlanReview.tsx:464–475` | ข้อ 5 |
| ข้อความเตือน PII 4 จุด | ตาราง §5 ข้อ 7 | ข้อ 7 |

### กอง B — ต้องมีตอนจบ Phase 5 แต่ยังไม่มีตอนนี้

| # | ของ | สถานะตอนนี้ | ทำได้ในขอบเขต visual? |
| --- | --- | --- | --- |
| B1 | **ป้ายปุ่ม copy บอกปลายทางชัด** — ปุ่มหนึ่งไประบบ SET ปุ่มหนึ่งไประบบคูปอง แยกกันเสมอ | ปุ่มมีแล้ว แต่ป้ายเป็น "คัดลอกเป้าหมายระยะสั้น" / "คัดลอกทั้งหมด" / "คัดลอก" ไม่ได้บอกชื่อระบบ (ปลายทาง SET ของเป้าหมายอ้างอิงจาก comment `CopyButton.tsx:3`) | ได้ — แก้ข้อความป้าย |
| B2 | **พื้นที่พรีวิวเอกสาร** (Sarabun, ดู §4.7) | ไม่มี | ไม่ได้ — เป็น element ใหม่ ต้องผ่าน decision rule ก่อน |
| B3 | **error ของ classifier แสดงเป็นแถบ `danger`** แยกจาก warning | `AssessmentForm.tsx:304–315` ใช้กล่อง amber เดียวกันทั้งกรณี "AI ไม่มั่นใจ" (warning) และ "จัดระดับไม่สำเร็จ/เชื่อมต่อไม่สำเร็จ" (error, state `err`) | ได้ — แยก className ตาม `err` ที่มีอยู่แล้ว |
| B4 | **error อื่นเป็นแถบ `danger` เต็มความกว้าง ไม่หายเอง** | `app/HomeClient.tsx:126`, `PlanReview.tsx:352`, `StudentPicker.tsx:354`, `app/students/[id]/StudentHistoryClient.tsx:44`, `app/auth/signin/page.tsx:43` ใช้ `red-*` แบบกล่องอ่อน/ข้อความเล็ก | ได้ |
| B5 | **⚠ inline ในตำแหน่งที่เกี่ยวข้อง** | `consistencyWarnings` เป็น `string[]` รวมที่หัว PlanReview (`:464–475`) ไม่มีข้อมูลว่าผูกกับช่อง/รายการไหน | ไม่ได้ — **ต้องแก้ `lib/types.ts` ให้ warning มีตำแหน่งผูก ซึ่งเป็นไฟล์ร่วม (CONTRACT frontend↔API) ต้องแจ้งทีมก่อน** ระหว่างนี้แสดงรวมที่หัวแบบเดิม |
| B6 | **chip "ยืนยัน" ในแถบที่ AI เสนอระดับความสามารถ** (44px ตาม §4.4) | ไม่มี — กรณี AI มั่นใจ (`confidence === "high"`) ระบบกรอกเป็นค่าที่ยืนยันแล้วทันที (`AssessmentForm.tsx:129–135`) ครูทำได้แค่กด "ไม่ใช่? แก้ระดับ" · ระหว่างนี้ใช้ tone กลางตาม §4.8 | ไม่ได้ — ต้องเพิ่มขั้นตอนกดยืนยัน (ขัด §5 ข้อ 8–9) ต้องตัดสินใจก่อน |
| B7 | **ฟอนต์ Sarabun** | ไม่ต้องโหลดจนกว่าจะมี B2 | — |
| B8 | **ปุ่มคัดลอกต่อรายการสื่อ แยกรายการที่ยังไม่ได้ตรวจให้เห็นชัด** — รายการที่ยังไม่ติ๊ก `isApproved` ใช้ tone `attention` + ⚠ + ป้ายบอกว่ายังไม่ได้ตรวจ (เช่น "⚠ คัดลอก · ยังไม่ได้ตรวจ") **แต่ยังกดได้** · **ห้าม disable ปุ่มหรือบล็อกการคัดลอก** — ขัด §5 ข้อ 5 (ห้าม auto-fix ครูตัดสินเอง) | `PlanReview.tsx:304` คัดลอก `mediaCopyText(media)` โดยไม่เช็ค `isApproved` และปุ่มหน้าตาเหมือนกันทุกรายการ → ครูคัดลอกสื่อที่ยังไม่ได้ตรวจไปวางในระบบคูปองได้ = คำขอเบิกงบที่ไม่มีใครอนุมัติ | ได้ — สลับ className/ข้อความป้ายตาม `media.isApproved` ที่มีอยู่แล้ว ไม่เพิ่ม handler หรือ state |

### บันทึกนอกขอบเขต visual (ไม่ใช่งาน Phase 5 แต่ต้องรู้)

**C1 — เมตริก ability override rate สูงเกินจริงเรื่องความแม่นของ classifier** · ✅ **แก้แล้ว 13 ก.ย. 2569 (PR #31)**

**ปัญหาเดิม** — กรณี `confidence === "high"` `AssessmentForm` คัดลอกค่า AI เข้า `abilityLevels` ทันทีโดยครูไม่ได้แตะ
และ state `manual` (ครูแตะเองหรือไม่) ไม่ถูกส่งไป API → `/api/stats` นับ domain ที่ครูไม่เคยดูว่า "ครูเห็นด้วย"
→ ความแม่นของ classifier สูงเกินจริงอย่างเป็นระบบ

**สิ่งที่แก้** (เพิ่มแบบ additive — client เก่าที่ไม่ส่งฟิลด์ใหม่ยังทำงานได้)

| ไฟล์ | เปลี่ยนอะไร |
| --- | --- |
| `lib/types.ts` | `CreatePlanRequest.abilityLevelsConfirmedByTeacher?: Record<string, boolean>` (optional) · `PlanUsageMetrics.abilityConfirmationBreakdown` |
| `prisma/schema.prisma` | `Assessment.abilityLevelsConfirmedByTeacher Json?` (nullable · ไม่ใช่ PII) |
| `components/AssessmentForm.tsx` | ส่ง `{ ...manual }` ไปกับ request — ไม่แตะตรรกะที่ตัดสินว่า `manual` เป็น true/false |
| `app/api/plans/route.ts` | บันทึกเฉพาะค่า boolean · ไม่ส่งมา = เก็บ `null` (ห้ามแทนด้วย `{}` เพราะ stats ใช้แยก "ไม่ทราบ") |
| `app/api/stats/route.ts` | แยกทุก domain ที่ AI เสนอเป็น 4 กอง: `teacherAgreed` / `teacherOverrode` / `notConfirmedByTeacher` / `unknown` · ตัวหารของ `abilityOverrideRate` = สองกองแรกเท่านั้น · แถวเก่า = `unknown` ห้ามเดา · แก้ comment ที่ไม่ตรงโค้ดด้วย |
| `app/stats/StatsClient.tsx` | หมายเหตุใต้การ์ดแสดงจำนวนของ 3 กอง แทนข้อความ "ยิ่งต่ำ = AI จัดระดับแม่น" |
| `scripts/test-api.mjs` | ขั้นที่ 5 อ่าน `stats.mine` และเช็คว่า domain ที่ครูไม่ได้แตะไปอยู่กอง `notConfirmedByTeacher` · ทุก request ส่งคุกกี้ `authjs.session-token` จาก `TEST_SESSION_TOKEN` (ไม่ตั้ง = หยุดพร้อมวิธีเอาค่า) — ไม่ได้ปิด auth check |

**ข้อจำกัดที่เหลืออยู่**

- **จนกว่า B6 (ปุ่ม "ยืนยัน") จะเสร็จ กอง `notConfirmedByTeacher` รวมคนสองกลุ่มที่แยกกันไม่ได้:**
  ครูที่ไม่เคยดูค่าที่ AI กรอกให้ และครูที่ดูแล้วเห็นด้วย (UI ไม่มีทางกดยืนยันโดยไม่เปลี่ยนค่า)
  → **ยังอ้าง `abilityOverrideRate` เป็น "ความแม่นของ classifier" ไม่ได้**
- ผลต่อตัวเลข: กองที่ครูแตะเอง (ตัวหาร) เอียงไปทางกรณีที่ครูไม่เห็นด้วย เพราะครูที่เห็นด้วยกับค่า AI มักไม่แตะ
  → `abilityOverrideRate` มีแนวโน้มสูงกว่าความจริง (ทิศตรงข้ามกับปัญหาเดิม) ต้องอ่านคู่กับทั้ง 4 กองเสมอ
- ข้อมูลที่บันทึกก่อน PR #31 อยู่กอง `unknown` ทั้งหมด แยกย้อนหลังไม่ได้

---

## 7. Checklist ก่อน merge

รันจากรากโปรเจกต์ **ทุกคำสั่งต้องคืนค่าว่างตอนจบ Phase 5**

```bash
# 1) class เดิมของ Tailwind ต้องหมดไปตอนจบ Phase 5
#    (เพิ่ม gray เพราะโค้ดมี text-gray-* อยู่ · ณ 13 ก.ย. 2569 คำสั่งเดิมไม่รวม gray เจอ 161 บรรทัด)
grep -rnE "(slate|gray|teal|amber|red)-[0-9]{2,3}" app components

# 2) rule ห้ามไปอยู่บนขอบ control
#    2a — จับ class ที่ประกาศเป็นตัวแปร เช่น const inputCls = "... border-rule ..."
grep -rn "border-rule" app components | grep -iE "input|textarea|select"
#    2b — จับ className ที่เขียนในแท็กตรงๆ ซึ่ง <input กับ className อยู่คนละบรรทัด (2a จับไม่ได้)
rg -nUP --multiline-dotall "<(input|textarea|select)\b(?:(?!/>|<[a-zA-Z]).)*?border-rule" app components

# 3) ขนาดตัวอักษรต่ำสุด 14px — text-xs ต้องหมด
grep -rn "text-xs" app components

# 4) shadow บน element ที่ไม่ได้ลอยจริงต้องเป็นศูนย์
#    ณ 13 ก.ย. 2569 เจอ 3 จุดที่ต้องเอาออก: app/auth/signin/page.tsx:36, app/auth/denied/page.tsx:35, app/privacy/page.tsx:45
#    AuthMenu.tsx ถูกกรองออกเพราะเป็น dropdown ที่อนุญาต (§4.1) — ถ้าเพิ่ม modal/sticky bar ใหม่ ให้เพิ่มไฟล์นั้นใน grep -v
grep -rnE "shadow(-[a-z]+)?\b" app components | grep -v "components/AuthMenu.tsx"
```

> ทำไมต้องมี 2b: ทดสอบกับ `border-slate-300` ตอนนี้ — มี 21 บรรทัด คำสั่ง 2a จับได้ 1 บรรทัด (`inputCls`)
> ส่วน control ที่เขียน className ในแท็กตรงๆ 10 ตัว จับได้ด้วย 2b เท่านั้น ถ้าใช้ 2a อย่างเดียวจะคืนค่าว่างทั้งที่ยังผิด

### หมายเหตุ: ผลของการเลิกใช้ `text-xs`

การยก **50 จุด** (ณ 13 ก.ย. 2569) จาก 12px เป็น 14px จะทำให้ข้อความตัดบรรทัดต่างจากเดิม
และหน้า `AssessmentForm` ที่ scroll ยาวอยู่แล้วจะยาวขึ้นอีก — **นี่คือผลที่ตั้งใจ ไม่ใช่บั๊ก**
แต่หมายความว่า Phase 5 ของหน้านั้นต้องไล่ดูทุก breakpoint (390px / 768px / 1024px / 1280px) ไม่ใช่เปลี่ยนสีแล้วจบ
