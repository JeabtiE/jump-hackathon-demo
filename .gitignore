# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ⚠️ env — ห้าม commit เด็ดขาด (API key หลุด = โดนใช้จนเงินหมด)
.env
.env.local
.env*.local

# ⚠️ ข้อมูลเด็กจริง — ห้าม commit
/data/real-cases/
*-real.json

# ไฟล์ .docx จาก test:api
/scripts/output/

# ⚠️ ต้นฉบับคู่มือฯ 2568 ที่สกัดจาก PDF — ไม่ commit (ไฟล์ใหญ่ + เป็นเอกสารต้นฉบับ)
# ใช้รัน scripts/parse_catalog.py เท่านั้น ผลลัพธ์คือ data/mediaCatalog2568.json ที่ commit แทน
/pdfs/

# vercel
.vercel

# typescript
*.tsbuildinfo

# prisma
/prisma/migrations/dev.db*
fetch_all_branches.bat 
mailmap.txt 
